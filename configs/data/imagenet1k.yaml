data:
  root: /data/imagenet
  train_list: null       # optional path to file list
  val_list: null         # optional path to file list
  num_workers: 8
  batch_size: 256
  shuffle: true
  pin_memory: true
  persistent_workers: true
  drop_last: true


[pytest]
addopts = -q
testpaths = tests
markers =
  cuda: requires CUDA
  slow: longer-running integration or property tests
  dist: uses torch.distributed
filterwarnings =
  ignore::DeprecationWarning
